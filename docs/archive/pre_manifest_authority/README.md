# Legacy Python Contract Archive

These files preserve the pre-manifest Python-authored TrueColor Phase 1
contract for historical comparison only.

They are not executable product authority and must not be imported by
production code or tests.

Canonical authority:

1. `qudipi.toml`
2. Rust validation and canonical compilation
3. `artifacts/stage_00/compiled_config_manifest.json`
4. Read-only consumers in Python, Go, TypeScript, and other runtimes

The archived Rust `lib.rs` files preserve the pre-modular Stage 0
implementation. They are historical references only and must not be compiled
or imported.
